name: Command via SSH

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Clean and checkout
        uses: garygrossgarten/github-action-ssh@release
        with:
          command: |
            sudo pkill -9 -f server.py && rm -rf gomoku && https://github.com/aczh/gomoku.git && cd gomoku && git checkout ${GITHUB_REF##*/}
          host: ec2-52-72-176-34.compute-1.amazonaws.com
          username: ubuntu
          passphrase: ${{ secrets.AWS_PASSWORD }}
          privateKey: ${{ secrets.AWS_SSH_SECRET_KEY}}
      - name: Deploy to AWS
        uses: garygrossgarten/github-action-ssh@release
        with:
          command: |
            cd gomoku && nohup sudo -b inv build run
          host: ec2-52-72-176-34.compute-1.amazonaws.com
          username: ubuntu
          passphrase: ${{ secrets.AWS_PASSWORD }}
          privateKey: ${{ secrets.AWS_SSH_SECRET_KEY}}
